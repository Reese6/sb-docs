---
title: <Название сервиса> — API
type: api
status: draft
version: 0.1
owners:
  - backend
related:
  - ../conventions.md
  - ../errors.md
---

# <Название сервиса> — API

<!-- AI: человекочитаемый контракт сервисного API — docs/api/services/<service>.md.
Файл создаёт только skill feature-apply из методов feature, которые шире одной
feature. Имя файла совпадает с именем сервиса в docs/architecture/components.md.
Ключа feature в frontmatter здесь нет: документ не принадлежит feature.
Контракт уровня feature живёт в docs/features/<feature-name>/api/ — файл на метод;
этот шаблон для него не используется.
Придумывать методы, поля и коды запрещено (rules/ai-guardrails.md): неизвестный
контракт — TBD. Один метод — один блок второго уровня «METHOD path». Блок
повторяется столько раз, сколько методов у сервиса; порядок подсекций не меняется.
Глобальные соглашения НЕ копировать, только ссылаться:
аутентификация — docs/api/authentication.md;
пагинация, фильтрация, сортировка, идемпотентность, лимиты частоты вызовов,
безопасность, форматы данных — docs/api/conventions.md;
формат ошибок и общие коды — docs/api/errors.md.
Неприменимые подсекции помечать «Not applicable: <причина>», не удалять.
Документ не превращать в OpenAPI без явного запроса. -->

## Purpose

<!-- AI: 1–2 предложения: за какую область отвечает сервис и кто его вызывает. -->

## Related requirements

<!-- AI: FR/BR/NFR features, которые реализует сервис. Только ID со ссылкой —
без копий формулировок. Область видимости ID глобальная (schemas/README.md). -->

## <METHOD> <path>

<!-- AI: заголовок — метод и путь как есть: «POST /v1/otp/request».
Первая строка блока — определение требования строго в форме
- API-001 (→ FR-012): <специфика уровня API>.
Только эта форма считается определением ID (schemas/README.md); в заголовке ID не ставить.
Новый ID = наибольший API-XXX в глобальной области (включая deprecated) + 1.
Одна строка на метод; несколько API-XXX у одного метода — только если это разные требования. -->

### Роли

<!-- AI: кто может вызывать метод. Идентификаторы ролей — дословно из
docs/product/roles.md, новые здесь не вводить: нужной роли нет — TBD и предложить
дополнить roles.md. Указать и то, кто вызывать не может, если это существенно. -->

### Параметры

<!-- AI: параметры пути, query и заголовки: | Имя | Тип | Обязательность | Описание |.
Расположение указывать в описании (path / query / header). Параметров нет —
«Not applicable». Общие форматы — docs/api/conventions.md. -->

### Request

<!-- AI: тело запроса: | Поле | Тип | Обязательность | Валидация | Ошибка при нарушении |.
Валидация — серверная; правила обязаны совпадать с ui.md feature-источника,
расхождение помечать конфликтом, не «чинить» молча.
Тела нет — «Not applicable: тело запроса не используется». -->

### Response

<!-- AI: успешный ответ: статус-код, затем | Поле | Тип | Описание |.
Несколько успешных статусов — строка на каждый с условием. Тело пустое — указать явно. -->

### Errors

<!-- AI: только специфичные для этого метода ошибки: | Статус | Code | Условие |.
Формат тела ошибки и общие коды — ссылка на docs/api/errors.md, не копировать.
Новый общий код — предложить в docs/api/errors.md, локально не заводить. -->

## Traceability

<!-- AI: таблица связей:
| API ID | Требование | Метод | Feature-источник |
Строка на каждый API-XXX, включая deprecated; минимум одна связь с FR/BR/NFR.
Feature-источник — ссылка на api/README.md той feature, из которой промоутирован метод. -->
