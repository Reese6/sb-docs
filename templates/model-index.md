---
title: <Название фичи> — модель данных
type: model
status: draft
feature: <feature-name>
version: 0.1
owners:
  - architecture
related:
  - ../product.md
  - ../requirements.md
---

# <Название фичи> — модель данных

<!-- AI: индекс модели данных feature — docs/features/<feature-name>/model/README.md.
Здесь только сводка: назначение, связанные требования, список сущностей,
диаграмма и трассируемость. Поля и дельта сущности — в отдельном файле
model/<entity>.md из templates/model-entity.md; сюда их не копировать.
Файл создаётся первым, до первого файла сущности.
Сначала переиспользовать сущности из docs/architecture/data-model/, только потом
заводить новые. Имена сущностей — из docs/product/glossary.md.
Придумывать сущности и поля запрещено (rules/ai-guardrails.md): неизвестное — TBD.
Физическая схема БД (таблицы, индексы, миграции) сюда не пишется — это technical.md.
Сущности ID не получают (schemas/README.md): идентификатор — имя файла сущности.
Неприменимые секции помечать «Not applicable: <причина>», не удалять. -->

## Назначение

<!-- AI: 1–2 предложения: какую часть требований покрывает модель данных feature. -->

## Связанные требования

<!-- AI: FR/BR/NFR, которые опираются на эти сущности. Только ID со ссылкой
на ../requirements.md — без копий формулировок. -->

## Существующие сущности

<!-- AI: сущности из docs/architecture/data-model/, которые feature использует
БЕЗ изменений. Только ссылка и роль в сценарии, своего файла такая сущность не получает:
- [Заявка](../../../architecture/data-model/claim.md) — хранит запрос пользователя.
Поля НЕ копировать. Сущность, которую feature меняет, идёт в таблицу ниже. -->

## Сущности feature

<!-- AI: таблица новых и изменяемых сущностей, строка на файл:
| Сущность | Файл | Источник |
| Код подтверждения | [otp-code.md](otp-code.md) | Новая |
| Пользователь | [user.md](user.md) | Изменение существующей |
Ни одной новой или изменяемой сущности — «Not applicable: feature не вводит и не меняет сущностей».
Строка без существующего файла даёт битую ссылку: строку добавлять вместе с файлом. -->

## Диаграмма

<!-- AI: Mermaid erDiagram в блоке mermaid: сущности этой feature и их связи.
Диаграмма не добавляет смысла (одна сущность без связей) — «Not applicable: <причина>». -->

## Трассируемость

<!-- AI: таблица «сущность → требование»:
| Сущность | Файл | Требование | Роль |
Каждая новая сущность и каждое изменение обязаны иметь минимум одну связь с FR/BR.
Связи нет — сущность не обоснована требованиями, это находка для review. -->
