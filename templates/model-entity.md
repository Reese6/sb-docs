---
title: <Сущность> в <название фичи>
type: model
status: draft
feature: <feature-name>
version: 0.1
owners:
  - architecture
related:
  - README.md
  - ../requirements.md
---

# Сущность: <Имя сущности>

<!-- AI: одна сущность уровня feature — один файл docs/features/<feature-name>/model/.
Файл заводится только на НОВУЮ сущность или на сущность, которую feature МЕНЯЕТ.
Сущность, используемая без изменений, файла не получает — только строка-ссылка
в секции «Существующие сущности» файла README.md.
Имя файла — kebab-case, латиница, совпадает с именем файла сущности в
docs/architecture/data-model/, если сущность там уже есть: otp-code.md, user.md.
Имя сущности в H1 и title — из docs/product/glossary.md; термина нет — TBD
и предложить дополнить glossary.md.
Придумывать поля и типы запрещено (rules/ai-guardrails.md): неизвестное — TBD.
Физическая схема БД (таблицы, индексы, миграции) сюда не пишется — это technical.md.
Сущности ID не получают (schemas/README.md): идентификатор — имя этого файла.
Файл в docs/architecture/data-model/ отсюда создаёт команда /feature-apply
после перевода документов в approved.
Неприменимые секции помечать «Not applicable: <причина>», не удалять.
После записи файла — строка сущности в таблицах «Сущности feature» и
«Трассируемость» файла README.md. -->

## Роль в feature

<!-- AI: 1–2 предложения: что сущность представляет и зачем нужна этой feature.
Без деталей хранения. -->

## Источник

<!-- AI: одна строка, ровно одна из двух форм:
«Новая: в docs/architecture/data-model/ сущности нет.»
«Изменение существующей: [Пользователь](../../../architecture/data-model/user.md).»
Совпадение по смыслу под другим именем — это существующая сущность, не новая. -->

## Поля

<!-- AI: только для НОВОЙ сущности: | Поле | Тип | Обязательность | Описание |.
Тип и обязательность — только подтверждённые; иначе TBD в ячейке.
Сущность существующая — «Not applicable: состав полей — в файле сущности, здесь только изменения». -->

## Изменения

<!-- AI: только для СУЩЕСТВУЮЩЕЙ сущности, только дельта:
ADDED: <поле> — <тип> — <обязательность> — <зачем>.
MODIFIED: <поле> — было <как> — стало <как> — <почему>.
Формы REMOVED нет: удаление поля — через change-management.
Сущность целиком не переописывать. Сущность новая — «Not applicable: сущность новая». -->

## Связи

<!-- AI: связи с другими сущностями: кардинальность и смысл, со ссылкой на файл:
- один ко многим с [Клиент](../../../architecture/data-model/client.md) — заявку подаёт один клиент.
Связей нет — «Not applicable: сущность автономна». -->

## Трассируемость

<!-- AI: с какими FR/BR связана сущность или её изменение:
| Требование | Роль сущности |
Минимум одна связь. Связи нет — TBD и вопрос человеку. -->
