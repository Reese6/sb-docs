---
title: <Название метода> — <METHOD> <path>
type: api
status: draft
feature: <feature-name>
version: 0.1
owners:
  - backend
related:
  - README.md
  - ../requirements.md
---

# <Название метода> — <METHOD> <path>

<!-- AI: один метод API feature — один файл docs/features/<feature-name>/api/.
Имя файла: <method>-<смысловое-имя>.md, kebab-case, латиница.
<method> — HTTP-метод в нижнем регистре; <смысловое-имя> — 1–3 слова из glossary
или из пути: post-otp-request.md, get-otp-status.md. Имя занято — уточнить
именем ресурса из пути. Имя файла не менять после создания.
H1 и title обязаны содержать русское название метода (валидатор MD-H1-LANG):
форма «Запрос кода — POST /v1/otp/request».
Источник контракта — ../requirements.md; придумывать методы, поля и коды
запрещено (rules/ai-guardrails.md): неизвестный контракт — TBD.
Глобальные соглашения НЕ копировать, только ссылаться:
аутентификация — docs/api/authentication.md;
пагинация, идемпотентность, лимиты частоты вызовов, форматы — docs/api/conventions.md;
формат ошибок и общие коды — docs/api/errors.md;
побочные эффекты, события, кэширование — technical.md feature.
Неприменимые секции помечать «Not applicable: <причина>», не удалять.
Документ не превращать в OpenAPI без явного запроса.
После записи файла — строка метода в таблицах «Методы» и «Traceability» файла README.md. -->

<!-- AI: первая строка тела — определение требования строго в форме
- API-001 (→ FR-012): <специфика уровня API>.
Только эта форма считается определением ID (schemas/README.md); в H1 ID не ставить.
Новый ID = наибольший API-XXX по всем файлам api/ (включая deprecated) + 1;
искать так: grep -rn "API-" docs/features/<feature-name>/api/.
Одна строка на метод; несколько API-XXX — только если это разные требования. -->

TBD: определение требования — первой строкой тела, по форме из комментария выше.

## Роли

<!-- AI: кто может вызывать метод. Идентификаторы ролей — дословно из
docs/product/roles.md, новые здесь не вводить: нужной роли нет — TBD и предложить
дополнить roles.md. Указать и то, кто вызывать не может, если это существенно. -->

## Параметры

<!-- AI: параметры пути, query и заголовки: | Имя | Тип | Обязательность | Описание |.
Расположение указывать в описании (path / query / header). Параметров нет —
«Not applicable». Общие форматы — docs/api/conventions.md. -->

## Request

<!-- AI: тело запроса: | Поле | Тип | Обязательность | Валидация | Ошибка при нарушении |.
Валидация — серверная; правила обязаны совпадать с ../ui.md, расхождение помечать
конфликтом, не «чинить» молча. Тела нет — «Not applicable: тело запроса не используется». -->

## Response

<!-- AI: успешный ответ: статус-код, затем | Поле | Тип | Описание |.
Несколько успешных статусов — строка на каждый с условием. Тело пустое — указать явно. -->

## Errors

<!-- AI: только специфичные для этого метода ошибки: | Статус | Code | Условие |.
Формат тела ошибки и общие коды — ссылка на docs/api/errors.md, не копировать.
Новый общий код — предложить в docs/api/errors.md, локально не заводить. -->
