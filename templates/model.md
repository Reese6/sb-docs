---
title: <Название фичи> — модель данных
type: model
status: draft
feature: <feature-name>
version: 0.1
owners:
  - architecture
related:
  - product.md
  - requirements.md
---

# <Название фичи> — модель данных

<!-- AI: доменная модель данных feature: какие сущности участвуют, какие
появляются, какие меняются. Источник — product.md и requirements.md.
Сначала переиспользовать сущности из docs/architecture/data-model/, только потом
заводить новые. Имена сущностей и полей — из docs/product/glossary.md.
Придумывать поля и типы запрещено (rules/ai-guardrails.md): неизвестное — TBD.
Физическая схема БД (таблицы, индексы, миграции) сюда не пишется — это technical.md.
Сущности ID не получают (schemas/README.md): идентификатор — имя файла сущности.
Неприменимые секции помечать «Not applicable: <причина>», не удалять. -->

## Назначение

<!-- AI: 1–2 предложения: какую часть требований покрывает модель данных feature. -->

## Связанные требования

<!-- AI: FR/BR/NFR, которые опираются на эти сущности. Только ID со ссылкой
на requirements.md — без копий формулировок. -->

## Существующие сущности

<!-- AI: сущности из docs/architecture/data-model/, которые feature использует
без изменений. Только ссылка и роль в сценарии:
- [Заявка](../../architecture/data-model/claim.md) — хранит запрос пользователя.
Поля НЕ копировать. Нужной сущности в data-model нет — она новая, её место ниже. -->

## Новые сущности

<!-- AI: сущность, которой ещё нет в docs/architecture/data-model/.
Подраздел третьего уровня на каждую сущность, заголовок — имя сущности из glossary.
Внутри: назначение одной фразой, таблица полей и связи.
Таблица полей: | Поле | Тип | Обязательность | Описание |.
Тип и обязательность — только подтверждённые; иначе TBD в ячейке.
Новых сущностей нет — «Not applicable: feature не вводит сущностей». -->

## Изменения существующих сущностей

<!-- AI: дельта к сущности, файл которой уже есть в docs/architecture/data-model/.
Подраздел третьего уровня на сущность, внутри — только изменения:
ADDED: <поле> — <тип> — <обязательность> — <зачем>.
MODIFIED: <поле> — было <как> — стало <как> — <почему>.
Сущность целиком не переописывать. Изменений нет — «Not applicable». -->

## Диаграмма

<!-- AI: Mermaid erDiagram в блоке mermaid: сущности этой feature и их связи.
Диаграмма не добавляет смысла (одна сущность без связей) — «Not applicable: <причина>». -->

## Трассируемость

<!-- AI: таблица «сущность → требование»:
| Сущность | Требование | Роль |
Каждая новая сущность и каждое изменение обязаны иметь минимум одну связь с FR/BR.
Связи нет — сущность не обоснована требованиями, это находка для review. -->
