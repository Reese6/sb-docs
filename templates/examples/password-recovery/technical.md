---
title: Восстановление пароля — техническое описание
type: technical
status: draft
feature: password-recovery
version: 0.2
owners:
  - backend
related:
  - requirements.md
  - api.md
---

# Восстановление пароля — техническое описание

## Context

Реализация восстановления пароля по [requirements.md](requirements.md) и контракту [api.md](api.md): сессии восстановления, генерация и проверка OTP, установка нового пароля.

## Related requirements

FR-001, FR-002, FR-003, FR-004, FR-005, BR-001, BR-002, BR-003, BR-004, BR-005, NFR-001, NFR-002 ([requirements.md](requirements.md)); API-001, API-002, API-003, API-004, API-005 ([api.md](api.md)).

## Current implementation

TBD: код сервиса аутентификации не подключён в services/ — текущая реализация не анализировалась.

## Proposed solution

Сервис аутентификации ведёт сессии восстановления с ограниченным сроком жизни (BR-001, BR-005). При запросе восстановления генерируется OTP, сохраняется только его хэш (NFR-002), открытое значение уходит в канал доставки email и нигде не сохраняется. Подтверждение сравнивает хэш введённого кода с сохранённым, проверяет срок действия (BR-005) и считает неверные попытки (BR-003). Успешное подтверждение выдаёт одноразовый `reset_token`, по которому устанавливается новый пароль и завершается сессия.

TBD: способ доставки email (провайдер/внутренний сервис рассылки) не определён.

## Components

TBD: реестр компонентов не заполнен — см. [../../architecture/components.md](../../architecture/components.md).

## Data flow

1. Клиент вызывает `POST /auth/password-recovery` (API-001).
2. Сервис находит учётную запись по email; при отсутствии — отвечает так же, как при наличии (BR-004), без отправки кода.
3. При активной сессии сервис проверяет интервал повторной отправки (BR-002): интервал не истёк — новый OTP не генерируется и не отправляется, ответ не отличается от успешного (API-004, API-005); иначе сервис создаёт или переиспользует активную сессию восстановления (BR-001), генерирует OTP, сохраняет хэш, инициирует отправку email.
4. Клиент вызывает `POST /auth/password-recovery/confirm` (API-002); сервис проверяет код, срок его действия (BR-005) и счётчик попыток (BR-003), при успехе выдаёт `reset_token`.
5. Клиент вызывает `POST /auth/password-recovery/reset` (API-003); сервис обновляет пароль и завершает сессию.

## Services involved

TBD: перечень сервисов не определён; код не подключён в services/.

## Data model changes

Новая сущность «сессия восстановления пароля»: идентификатор учётной записи, хэш OTP, счётчик неверных попыток, время создания и истечения, состояние. Детали схемы — TBD (см. [../../architecture/data-model.md](../../architecture/data-model.md)).

## API changes

Три новых endpoint — контракт в [api.md](api.md). Внутренних API-изменений нет.

## Events

TBD: события не определены (см. Events в api.md).

## Caching

Not applicable: кэширование не требуется.

## Transactions

Смена пароля и завершение сессии восстановления выполняются атомарно. Отправка email выполняется вне транзакции; сбой отправки не оставляет сессию в несогласованном состоянии.

## Error handling

- Сбой отправки email: TBD: политика повторов не определена.
- Проверка OTP и счётчик попыток должны быть устойчивы к конкурентным запросам confirm (BR-003 не обходится параллельными вводами).

## Security

- Хранение только хэша OTP, запрет логирования открытого значения (NFR-002).
- `reset_token` одноразовый, с ограниченным сроком жизни. TBD: срок жизни `reset_token`.
- Защита от перечисления пользователей — единообразный ответ (BR-004, API-005).

## Observability

TBD: стандарт метрик и алертов не определён. Кандидаты: счётчики запросов восстановления, доля неуспешных подтверждений, время доставки email (NFR-001).

## Performance

Нагрузка низкая относительно основного трафика аутентификации. Целевое время доставки OTP — NFR-001.

## Backward compatibility

Not applicable: новая функциональность, существующие контракты не меняются.

## Migration

Добавление хранилища сессий восстановления. Обратная миграция — удаление хранилища; данные сессий временные.

## Deployment

TBD: порядок выката и фиче-флаги не определены.

## Testing considerations

- Интеграционные сценарии: полный проход восстановления, повторная отправка (BR-002), исчерпание попыток (BR-003), несуществующий email (BR-004).
- Мок канала доставки email.
- Проверка отсутствия открытого OTP в логах (NFR-002).

## Alternatives

Выбор механизма подтверждения (OTP против ссылки восстановления) — [decisions/adr-001-otp-vs-recovery-link.md](decisions/adr-001-otp-vs-recovery-link.md).

## Risks

- Недоставка email пользователю — восстановление невозможно; смягчение: повторная отправка (FR-005).
- Перебор OTP при слабых лимитах; смягчение: BR-003 и rate limits (TBD в api.md).

## Open questions

- TBD: способ доставки email.
- TBD: срок жизни `reset_token`.
- ASSUMPTION: срок жизни сессии восстановления равен сроку жизни OTP (BR-005; 5 минут — см. ASSUMPTION в requirements.md).
  Requires confirmation.
