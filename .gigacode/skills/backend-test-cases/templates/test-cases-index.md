# <Название фичи или change> — тест-кейсы backend

<!-- AI: индекс тест-кейсов backend — tmp/test-cases/<feature-or-change>/README.md
(gitignored, вне репо, не часть doc-pipeline — не проверяется validate-docs.mjs).
<feature-or-change> — имя feature либо имя директории change.
Здесь только сводка: область проверки, таблица кейсов, матрица покрытия,
открытые вопросы, очередь операций.
Содержание кейса — в отдельном файле
tmp/test-cases/<feature-or-change>/tc-<NNN>-<имя>.md из
.gigacode/skills/backend-test-cases/templates/test-case.md; сюда его
не копировать. Индекс содержимое кейса не дублирует: ID берётся из определения
первой строкой тела кейса.
Индекс, у которого пока нет ни одного файла кейса, — допустимое состояние, если
для кейсов нет оснований (см. секцию «Открытые вопросы»); отличие от случайно
пустой директории — заполненная секция «Область проверки» и явная причина в
«Открытых вопросах», а не пустой файл. -->

## Область проверки

<!-- AI: что покрывает набор: feature, операции или сценарии; версии использованных
источников (requirements.md, api/, technical.md — version из их frontmatter);
явные исключения — что сознательно не проверяется и почему.
Набор по change: перечислить дельты (ADDED/MODIFIED/REMOVED к каждому целевому
документу) и затронутые существующие требования из секции Impact proposal.md;
поведение feature вне дельт и Impact — в исключения. -->

TBD: область проверки не определена.

## Кейсы

<!-- AI: таблица всех файлов набора, строка на файл:
| TC | Файл | Операция, сценарий или дельта | Цель |
| TC-001 | [tc-001-...](tc-001-...md) | POST /example/items | ... | -->

TBD: кейсы не описаны.

## Покрытие

<!-- AI: матрица «источник и критерий → сценарий → TC и вариант → covered /
missing / not-applicable»:
| Источник (FR/BR/NFR/API, для change — дельта или FR-NEW-<n>) | Сценарий | TC (вариант) | Статус покрытия |
missing — сценарий определён, TC ещё нет; not-applicable — обязательна причина в
этой же строке. Без определённой области выше — не писать «полное покрытие» и
процент покрытия. -->

TBD: матрица покрытия не построена.

## Открытые вопросы

<!-- AI: что неизвестно, источник проблемы (файл, секция), зависимые кейсы, кто
должен уточнить. TBD и ASSUMPTION — с указанием файла и секции. -->

## Очередь операций

<!-- AI: заполнять только когда запрошена вся feature или весь change целиком:
завершённые, текущая, оставшиеся операции и следующий шаг. Для change единица
очереди — дельта. Один вызов — одна единица; при обрыве контекста продолжение
опирается на эту таблицу, а не на память чата. -->

Not applicable: запрошена одна операция, дельта или сценарий, не весь набор.
