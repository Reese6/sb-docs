# <Название кейса>

<!-- AI: один тест-кейс backend — один файл tmp/test-cases/<feature-or-change>/, где
<feature-or-change> — имя feature либо имя директории change. Имя файла:
tc-<NNN>-<смысловое-имя>.md, kebab-case, латиница, <NNN> с ведущими нулями. Источник
поведения — requirements.md, api/, technical.md, модель данных, ADR, дельта
docs/changes/<change-name>/proposal.md или слова человека; services/ не анализировать
(AGENTS.md). Придумывать поведение, данные подготовки и способ наблюдения запрещено
(rules/ai-guardrails.md): неизвестное — TBD, спорное — ASSUMPTION: ... Requires
confirmation. Неприменимые секции помечать «Not applicable: <причина>», не удалять.
После записи файла — строка кейса в таблицах «Кейсы» и «Покрытие»
tmp/test-cases/<feature-or-change>/README.md. -->

<!-- AI: первая строка тела — определение строго в форме
- TC-001 (→ FR-XXX/API-XXX/scenario): <проверяемая цель одной фразой>.
Для дельты change — форма - TC-001 (→ FR-NEW-1, proposal <change-name>): ...
Только эта форма считается определением ID (schemas/README.md, соглашение — не
проверяется валидатором). Новый ID = наибольший TC-XXX по всем файлам
tmp/test-cases/<feature-or-change>/ + 1; искать так:
grep -rn "TC-" tmp/test-cases/<feature-or-change>/. -->

TBD: определение кейса — первой строкой тела, по форме из комментария выше.

## Цель

<!-- AI: одна проверяемая цель; уровень проверки (API-контракт / бизнес-операция /
взаимодействие компонентов / сквозной backend-сценарий); приоритет — предложение
тестировщика с причиной, шкала команды, если есть, иначе TBD: шкала приоритетов. -->

## Источники

<!-- AI: относительные ссылки на FR/BR/NFR/API либо секцию источника; статус и
версия каждого документа feature. Источник из change — ссылка на
docs/changes/<change-name>/proposal.md и конкретная дельта (ADDED/MODIFIED/REMOVED
к целевому документу); статус change не указывать. Источники противоречат друг
другу — зафиксировать оба и конфликт, не выбирать значение молча. -->

## Предусловия

<!-- AI: начальное состояние системы, участник и его права, окружение, внешние
зависимости. -->

## Тестовые данные

<!-- AI: точные значения либо однозначные правила их построения; связанные записи
и переменные. Различать отсутствие поля, null, пустую строку, пустой массив и
ноль. Синтетические данные — в границах источника, помечены как тестовые, не как
значения из требований. Набор однородных значений — таблицей вариантов:
| Вариант | Вход | Ожидаемый результат |
Каждый вариант — стабильный ключ, точный вход, свой ожидаемый результат. -->

## Подготовка

<!-- AI: как создать исходное состояние и как убедиться, что оно получено —
документированным интерфейсом или предоставленным механизмом тестового
окружения. Не изобретать вспомогательные endpoints, SQL-таблицы и команды. -->

## Действия

<!-- AI: нумерованный список. Для HTTP — метод, документированный путь,
path/query-параметры, заголовки, body (валидный JSON; намеренно некорректное
тело — пометить явно, точные байты в блоке text). Для сообщения — канал, ключ,
metadata, payload. Для переменной — происхождение: фиксированные данные,
результат подготовки или поле ответа конкретного шага; неопределённые
<valid-id>/<token> оставлять только как TBD с указанием недостающего
источника. -->

## Ожидаемый результат

<!-- AI: отдельные проверяемые утверждения формы «наблюдаемый объект/путь →
сравнение → ожидаемое значение или правило → источник», привязанные к действиям.
Для HTTP одной проверки статуса недостаточно, если контракт определяет body или
эффекты. Только контрактные гарантии: не сравнивать весь ответ, если допустимы
изменяемые или дополнительные поля. Побочный эффект — способ наблюдения
(документированное чтение состояния, событие, разрешённое тестовое хранилище).
Асинхронный результат — условие завершения, предел ожидания, способ опроса,
корреляция с вызовом; произвольный sleep не заменяет условие готовности.
Конкурентный вызов или повтор — допустимые исходы и конечный инвариант. -->

## Очистка

<!-- AI: что восстановить и каким доступным способом, включая случай ошибки
теста. -->

## Автоматизация

<!-- AI: зависимости, изоляция данных/заглушек/часов, управление временем,
способ наблюдения, возможность параллельного запуска; если невозможен —
необходимая изоляция или сериализация. Кейс не зависит от порядка запуска
других кейсов. -->

## Открытые вопросы

<!-- AI: TBD, ASSUMPTION, конфликты источников и условие снятия блокировки. -->
