---
title: Ошибки API
type: api
status: draft
version: 0.1
owners:
  - backend
---

# Ошибки API

Единый формат ошибок для всех API. Feature `api.md` описывает только специфичные для endpoint ошибки; формат и общие коды — здесь, один раз.

## Формат ответа об ошибке

TBD: структура тела ошибки (например, `code`, `message`, `details`, `traceId`).

## Общие коды ошибок

TBD: таблица общих ошибок (400/401/403/404/409/422/429/500) с семантикой.

## Правила

- Ошибки не раскрывают внутренние детали реализации (stack trace, SQL).
- Сообщения об ошибках стабильны: клиенты могут на них полагаться по `code`, не по тексту.
- Новый общий код добавляется сюда; специфичный для endpoint — в `api.md` его feature со ссылкой на этот документ.
