---
title: Соглашения API
type: api
status: draft
version: 0.1
owners:
  - backend
---

# Соглашения API

Единые соглашения для всех API продукта. Endpoint, нарушающий соглашение, должен иметь явное обоснование (ADR или пометка в своём `api.md`).

## Нейминг

TBD: правила именования ресурсов, путей, параметров (например, kebab-case в путях, camelCase в JSON).

## Версионирование

TBD: схема версионирования API (в пути /v1/, в заголовке и т.п.) и политика обратной совместимости.

## Пагинация

TBD: единая схема пагинации (cursor/offset), названия параметров, формат ответа.

## Фильтрация и сортировка

TBD: соглашения по параметрам фильтрации и сортировки.

## Форматы данных

TBD: формат дат (ISO 8601), денежных сумм, идентификаторов, кодировка.

## Идемпотентность

TBD: требования к идемпотентности мутирующих операций (Idempotency-Key и т.п.).

## Лимиты частоты вызовов

TBD: общие лимиты частоты вызовов, заголовки ответа и поведение при превышении. Специфика endpoint — в `api.md` его feature.

## Безопасность

TBD: общие требования безопасности API (чувствительные данные в ответах, маскирование, защита от перебора). Технические меры конкретного решения — в `technical.md` feature.
